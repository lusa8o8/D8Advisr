export default function SignupPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 px-4 text-center">
      <h1 className="text-3xl font-semibold text-text-primary">Sign up (placeholder)</h1>
      <p className="text-text-secondary">
        A dedicated onboarding flow will live here when Slice 2 adds Supabase auth.
      </p>
    </main>
  );
}
